---
phase: 15-claude-md-sentinel-variants
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/burrow/lib/core.cjs
  - .claude/burrow/lib/config.cjs
  - .claude/burrow/lib/installer.cjs
  - test/config.test.cjs
  - test/installer.test.cjs
autonomous: true
requirements: [SNP-01, SNP-02]

must_haves:
  truths:
    - "generateSnippet({loadMode:'full',...}) returns snippet with 'read `.planning/burrow/cards.json`' instruction"
    - "generateSnippet({loadMode:'index',...}) returns snippet with 'burrow index' instruction"
    - "generateSnippet({loadMode:'none',...}) returns snippet with 'skip context loading' instruction"
    - "generateSnippet({loadMode:'auto',...}) returns snippet with auto-threshold instruction"
    - "Trigger words from config appear in the snippet's trigger section"
    - "triggerPreset='none' causes trigger section to be omitted from snippet"
    - "atomicWriteFile() writes content via tmp+rename pattern"
  artifacts:
    - path: ".claude/burrow/lib/core.cjs"
      provides: "atomicWriteFile() function"
      exports: ["atomicWriteFile"]
    - path: ".claude/burrow/lib/config.cjs"
      provides: "triggerWords and triggerPreset in CONFIG_SCHEMA"
      contains: "triggerPreset"
    - path: ".claude/burrow/lib/installer.cjs"
      provides: "generateSnippet() function replacing CLAUDE_MD_SNIPPET"
      exports: ["generateSnippet"]
  key_links:
    - from: ".claude/burrow/lib/installer.cjs"
      to: ".claude/burrow/lib/config.cjs"
      via: "generateSnippet reads config.triggerPreset and config.triggerWords"
      pattern: "triggerPreset|triggerWords"
---

<objective>
Add atomicWriteFile() to core.cjs, expand CONFIG_SCHEMA with trigger word settings, and create generateSnippet(config) that produces mode-specific CLAUDE.md sentinel content.

Purpose: Replace the hardcoded CLAUDE_MD_SNIPPET constant with a dynamic function that tailors the sentinel block to the user's configured loadMode and trigger words.
Output: atomicWriteFile() utility, expanded config schema, generateSnippet() function with tests.
</objective>

<execution_context>
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-claude-md-sentinel-variants/15-CONTEXT.md

<interfaces>
<!-- From .claude/burrow/lib/core.cjs -->
```javascript
function atomicWriteJSON(filePath, data) { /* backup, write tmp, rename */ }
module.exports = { ensureDataDir, generateId, atomicWriteJSON };
```

<!-- From .claude/burrow/lib/config.cjs -->
```javascript
const DEFAULTS = { loadMode: 'auto', autoThreshold: 4000 };
const CONFIG_SCHEMA = {
  loadMode: { type: 'string', values: ['full', 'index', 'none', 'auto'] },
  autoThreshold: { type: 'number', validate: (v) => Number.isInteger(v) && v > 0, validateMsg: 'must be a positive integer' },
};
module.exports = { load, save, get, set, list, configPath, CONFIG_SCHEMA, DEFAULTS };
```

<!-- From .claude/burrow/lib/installer.cjs -->
```javascript
const SENTINEL_START = '<!-- burrow:start -->';
const SENTINEL_END = '<!-- burrow:end -->';
const CLAUDE_MD_SNIPPET = `## Burrow — Agent Memory\n\nBurrow is your persistent memory...`;
module.exports = { SENTINEL_START, SENTINEL_END, CLAUDE_MD_SNIPPET, detect, performInstall, performUpgrade, performRepair, writeSentinelBlock, removeSentinelBlock };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: atomicWriteFile in core.cjs + CONFIG_SCHEMA expansion</name>
  <files>.claude/burrow/lib/core.cjs, .claude/burrow/lib/config.cjs, test/config.test.cjs</files>
  <read_first>
    - .claude/burrow/lib/core.cjs (current atomicWriteJSON pattern to parallel)
    - .claude/burrow/lib/config.cjs (current CONFIG_SCHEMA, DEFAULTS, validateKey)
    - test/config.test.cjs (existing test patterns)
  </read_first>
  <behavior>
    - atomicWriteFile('/tmp/test.txt', 'hello') creates file with content 'hello'
    - atomicWriteFile on existing file creates .bak backup before writing
    - atomicWriteFile writes to .tmp then renames (no partial writes)
    - CONFIG_SCHEMA.triggerPreset has type 'string' with values ['broad', 'minimal', 'none', 'custom']
    - CONFIG_SCHEMA.triggerWords has type 'array'
    - DEFAULTS.triggerPreset equals 'broad'
    - DEFAULTS.triggerWords equals ["remember", "don't forget", "always do X", "note this", "save this", "keep track of", "burrow this"]
    - config.set(cwd, 'triggerPreset', 'minimal') persists and config.get returns 'minimal'
    - config.set(cwd, 'triggerPreset', 'invalid') throws with valid values listed
    - config.set(cwd, 'triggerWords', '["burrow this"]') parses JSON array and persists
    - config.set(cwd, 'triggerWords', 'not-json') throws with descriptive error
  </behavior>
  <action>
    1. In core.cjs, add atomicWriteFile(filePath, content) function AFTER atomicWriteJSON:
       - Same pattern: backup existing to .bak, write to .tmp, rename to target
       - Content is written as utf-8 string (no JSON serialization)
       - Per D-10, this is a separate function from atomicWriteJSON, NOT a shared helper
       - Export atomicWriteFile in module.exports

    2. In config.cjs, expand DEFAULTS object to add:
       - triggerPreset: 'broad'
       - triggerWords: ["remember", "don't forget", "always do X", "note this", "save this", "keep track of", "burrow this"]

    3. In config.cjs, expand CONFIG_SCHEMA to add:
       - triggerPreset: { type: 'string', values: ['broad', 'minimal', 'none', 'custom'] }
       - triggerWords: { type: 'array', validate: (v) => Array.isArray(v) && v.every(w => typeof w === 'string'), validateMsg: 'must be an array of strings' }

    4. In config.cjs set() function, add array type handling:
       - When schema.type === 'array', parse rawValue as JSON (it arrives as string from CLI)
       - If JSON.parse fails or result is not valid per schema.validate, throw descriptive error
       - This parallels the existing number coercion block for type === 'number'

    5. Per D-16: When triggerPreset is NOT 'custom', the triggerWords value is derived from preset at read time (not stored). Add a TRIGGER_PRESETS constant:
       ```javascript
       const TRIGGER_PRESETS = {
         broad: ["remember", "don't forget", "always do X", "note this", "save this", "keep track of", "burrow this"],
         minimal: ["burrow this"],
         none: [],
       };
       ```
       In load(), after merging defaults, if triggerPreset is not 'custom', override triggerWords with TRIGGER_PRESETS[triggerPreset].

    6. Write tests in test/config.test.cjs for new schema keys and array type handling.
  </action>
  <verify>
    <automated>cd /Users/mrmatos6837/Projects/personal/burrow && node --test test/config.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - core.cjs contains `function atomicWriteFile(filePath, content)`
    - core.cjs module.exports contains `atomicWriteFile`
    - config.cjs DEFAULTS contains `triggerPreset: 'broad'`
    - config.cjs DEFAULTS contains `triggerWords:` with array value
    - config.cjs CONFIG_SCHEMA contains `triggerPreset:` entry with values array
    - config.cjs CONFIG_SCHEMA contains `triggerWords:` entry with type 'array'
    - config.cjs contains `TRIGGER_PRESETS` constant with broad/minimal/none keys
    - config.cjs set() handles `type === 'array'` with JSON.parse
    - config.cjs load() overrides triggerWords from preset when triggerPreset !== 'custom'
    - test/config.test.cjs contains tests for triggerPreset and triggerWords
    - `node --test test/config.test.cjs` exits 0
  </acceptance_criteria>
  <done>atomicWriteFile exported from core.cjs; CONFIG_SCHEMA has triggerPreset and triggerWords with validation; preset-based derivation works in load(); all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateSnippet(config) in installer.cjs</name>
  <files>.claude/burrow/lib/installer.cjs, test/installer.test.cjs</files>
  <read_first>
    - .claude/burrow/lib/installer.cjs (current CLAUDE_MD_SNIPPET constant, full file)
    - test/installer.test.cjs (existing test patterns)
    - .claude/burrow/lib/config.cjs (DEFAULTS and TRIGGER_PRESETS for reference)
  </read_first>
  <behavior>
    - generateSnippet({loadMode:'full', triggerPreset:'broad', triggerWords:[...]}) contains 'read `.planning/burrow/cards.json`'
    - generateSnippet({loadMode:'index', triggerPreset:'broad', triggerWords:[...]}) contains 'burrow index'
    - generateSnippet({loadMode:'index', ...}) contains 'lazy body fetching' or 'drill down' instruction
    - generateSnippet({loadMode:'none', triggerPreset:'broad', triggerWords:[...]}) contains 'skip' or 'on demand'
    - generateSnippet({loadMode:'auto', triggerPreset:'broad', triggerWords:[...]}) contains 'auto' and 'threshold'
    - All modes include '## Burrow' heading
    - All modes include 'Privacy:' section
    - All modes include 'mutations go through the CLI'
    - generateSnippet with triggerPreset='broad' includes "remember", "don't forget"
    - generateSnippet with triggerPreset='none' does NOT include trigger word section
    - generateSnippet with triggerPreset='minimal' includes "burrow this" but NOT "remember"
    - generateSnippet with triggerPreset='custom' and triggerWords=['foo'] includes 'foo'
  </behavior>
  <action>
    1. In installer.cjs, DELETE the `const CLAUDE_MD_SNIPPET = ...` constant (lines 13-25).

    2. Create `generateSnippet(config)` function that takes a config object (with at minimum loadMode, triggerPreset, triggerWords). Per D-08, it takes full config, not just loadMode.

    3. The function builds a snippet string with these sections:
       a. HEADING (constant across all modes):
          ```
          ## Burrow — Agent Memory

          Burrow is your persistent memory.
          ```

       b. LOAD INSTRUCTION (varies by config.loadMode per D-01):
          - `full`: "On every session start, silently read `.planning/burrow/cards.json` using the Read tool to load project context, tasks, and notes."
          - `index`: "On every session start, run `node .claude/burrow/burrow-tools.cjs index --json` to load a lightweight card index (titles and IDs only, no bodies). When you need a card's full content, read `.planning/burrow/cards.json` and locate it by ID." (per D-03, no --depth flag)
          - `none`: "Burrow cards are available but not loaded at session start. When you need project context, tasks, or notes, read `.planning/burrow/cards.json` on demand."
          - `auto`: "On every session start, check the size of `.planning/burrow/cards.json`. If under the configured threshold, read it in full using the Read tool. If over, run `node .claude/burrow/burrow-tools.cjs index --json` for a lightweight index instead. When using index mode, read the full file to get card bodies on demand."

       c. TRIGGER SECTION (varies by triggerPreset per D-07):
          - If triggerPreset is 'none' OR triggerWords is empty array: OMIT this section entirely
          - Otherwise: Build trigger instruction with the actual words from triggerWords array:
            ```
            **When the user says {quoted word list}, or any instruction meant to persist across sessions:**
            - Store it as a Burrow card: `node .claude/burrow/burrow-tools.cjs add --title "<summary>" --body "<details>"`
            - Organize under relevant parent cards (create parents if needed)
            - Do NOT write to loose markdown files or other ad-hoc storage — Burrow is the single source of truth
            ```
          - The {quoted word list} formats each trigger word in quotes separated by commas: "remember", "don't forget", etc.

       d. FOOTER (constant across all modes per D-02):
          ```
          **Privacy:** Burrow data is meant to be committed to git. Anything stored in cards is visible to anyone with repo access. Avoid storing secrets, credentials, or sensitive personal information.

          All mutations go through the CLI — NEVER edit cards.json directly.
          ```

    4. Remove CLAUDE_MD_SNIPPET from module.exports. Add generateSnippet to module.exports.

    5. Write tests in test/installer.test.cjs for generateSnippet covering all 4 modes and trigger word variants.
  </action>
  <verify>
    <automated>cd /Users/mrmatos6837/Projects/personal/burrow && node --test test/installer.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - installer.cjs does NOT contain `const CLAUDE_MD_SNIPPET`
    - installer.cjs contains `function generateSnippet(config)`
    - installer.cjs module.exports contains `generateSnippet` and does NOT contain `CLAUDE_MD_SNIPPET`
    - generateSnippet({loadMode:'full',triggerPreset:'broad',triggerWords:["remember"]}) includes 'cards.json' and 'Read tool'
    - generateSnippet({loadMode:'index',triggerPreset:'broad',triggerWords:["remember"]}) includes 'burrow-tools.cjs index'
    - generateSnippet({loadMode:'none',triggerPreset:'broad',triggerWords:["remember"]}) includes 'on demand'
    - generateSnippet({loadMode:'auto',triggerPreset:'broad',triggerWords:["remember"]}) includes 'threshold'
    - generateSnippet({loadMode:'full',triggerPreset:'none',triggerWords:[]}) does NOT include 'When the user says'
    - All modes include '## Burrow' and 'Privacy:' and 'NEVER edit cards.json directly'
    - `node --test test/installer.test.cjs` exits 0
  </acceptance_criteria>
  <done>generateSnippet(config) produces distinct, correct snippets per loadMode; trigger words configurable; CLAUDE_MD_SNIPPET removed; all tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/config.test.cjs` passes (schema expansion, array handling, preset derivation)
- `node --test test/installer.test.cjs` passes (generateSnippet all modes, trigger variants)
- `grep -c 'CLAUDE_MD_SNIPPET' .claude/burrow/lib/installer.cjs` returns 0
- `grep -c 'generateSnippet' .claude/burrow/lib/installer.cjs` returns at least 2 (function + export)
- `grep -c 'atomicWriteFile' .claude/burrow/lib/core.cjs` returns at least 2 (function + export)
</verification>

<success_criteria>
- generateSnippet(config) produces 4 distinct snippets for full/index/none/auto modes
- Trigger words are configurable via triggerPreset and triggerWords config keys
- atomicWriteFile() available in core.cjs for Plan 02 to use
- CLAUDE_MD_SNIPPET constant eliminated from codebase
- All existing tests still pass, new tests cover new functionality
</success_criteria>

<output>
After completion, create `.planning/phases/15-claude-md-sentinel-variants/15-01-SUMMARY.md`
</output>
