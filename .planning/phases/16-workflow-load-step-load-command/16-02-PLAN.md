---
phase: 16-workflow-load-step-load-command
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .claude/burrow/workflows/burrow.md
autonomous: true
requirements: [WFL-01, WFL-02, WFL-03, WFL-04, WFL-05, WFL-06]

must_haves:
  truths:
    - "Workflow Step 1 (LOAD) calls burrow load via Bash instead of Read tool"
    - "Agent knows which mode was resolved from the JSON envelope"
    - "Workflow documents lazy body-fetching pattern for index mode"
    - "Full mode behavior is functionally equivalent to current behavior"
    - "None mode instructs agent to skip loading and note cards are available on demand"
  artifacts:
    - path: ".claude/burrow/workflows/burrow.md"
      provides: "Updated LOAD step with burrow load dispatcher"
      contains: "burrow load"
  key_links:
    - from: ".claude/burrow/workflows/burrow.md"
      to: ".claude/burrow/burrow-tools.cjs"
      via: "node .claude/burrow/burrow-tools.cjs load"
      pattern: "burrow-tools\\.cjs load"
---

<objective>
Rewrite the workflow LOAD step to use `burrow load` as the universal dispatcher, replacing the direct Read tool approach. Document the lazy body-fetching pattern for index mode.

Purpose: This connects the config system to the agent's actual behavior — the workflow now branches based on loadMode config.
Output: Updated workflows/burrow.md with mode-aware LOAD step.
</objective>

<execution_context>
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- JSON envelope returned by burrow load -->
Full mode:    {"mode": "full", "cardCount": 42, "data": {"version": 2, "cards": [...]}}
Index mode:   {"mode": "index", "cardCount": 42, "data": {"cards": [{"id": "abc", "title": "...", "childCount": 3, "hasBody": true, "archived": false, "children": [...]}]}}
None mode:    {"mode": "none", "cardCount": 42}
Auto mode:    resolves to full or index — mode field reflects the resolved choice
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite workflow LOAD step for burrow load dispatcher</name>
  <files>.claude/burrow/workflows/burrow.md</files>
  <read_first>
    .claude/burrow/workflows/burrow.md
    .planning/phases/16-workflow-load-step-load-command/16-CONTEXT.md
  </read_first>
  <action>
    Rewrite Step 1 (LOAD) in `.claude/burrow/workflows/burrow.md`. Replace the current content of the "### Step 1: LOAD (silent)" section with the following. Keep the heading unchanged. Replace everything from after the heading to before "### Step 2: THINK".

    New Step 1 content:

    ```markdown
    Run `burrow load` via the Bash tool to load context. This command reads config.json and returns a JSON envelope with mode-appropriate data.

    ```
    node .claude/burrow/burrow-tools.cjs load
    ```

    The output is a JSON object: `{"mode": "<resolved>", "cardCount": N, "data": {...}}`

    **Mode behaviors:**

    - **full** (`mode: "full"`): `data` contains the complete card tree. Parse it into agent memory as the full card state. This is equivalent to the old Read tool approach.
    - **index** (`mode: "index"`): `data` contains a lightweight index (titles, IDs, child counts — no bodies). Parse it for card awareness. When you need a card's body content, use `node .claude/burrow/burrow-tools.cjs read <id> --full` to fetch it on demand (lazy body-fetching pattern).
    - **none** (`mode: "none"`): No `data` field. `cardCount` tells you how many cards exist. Skip loading entirely — cards are available on demand via `burrow read` or `/burrow`. Note this to yourself and proceed.
    - **auto**: Resolves to `full` or `index` based on file size vs threshold. The `mode` field reflects the resolved choice — handle it as full or index accordingly.

    **Skip if already loaded**: If the agent has already run `burrow load` in this conversation and no mutation has occurred since, skip this step.

    **After mutation**: Re-run `burrow load` to refresh agent memory (replaces the old "re-read cards.json" instruction).

    **Lazy body-fetching (index mode)**: In index mode, you have card titles and structure but not body content. When a user request requires a card's body (e.g., reading notes, quoting content), run `node .claude/burrow/burrow-tools.cjs read <id> --full` to get the full card. This keeps initial context lightweight while providing full access on demand.
    ```

    Also update the worked examples that reference the LOAD step. In Examples 1-8, replace all instances of:
    - `Read \`.planning/burrow/cards.json\` using the Read tool (silent).` or similar Read tool instructions
    - with: `Run \`burrow load\` via Bash (silent). Parse the JSON envelope.`

    Also update the re-LOAD instruction in mutation examples (Examples 5, 6). Replace:
    - `re-LOAD by reading \`cards.json\` with the Read tool` or similar
    - with: `re-LOAD by running \`burrow load\` via Bash`

    Update the "Command Reference" table to add a new row:
    | load | `load` | Load context per config. Returns JSON envelope. |

    Do NOT change Step 2 (THINK) or Step 3 (EXECUTE) logic — only update the LOAD references within them and in the examples.

    Per D-09, D-10, D-11: The workflow now uses Bash instead of Read tool, parses the JSON envelope, and handles none mode appropriately.

    Per WFL-06: The lazy body-fetching pattern is explicitly documented in the LOAD step.
  </action>
  <verify>
    <automated>cd /Users/mrmatos6837/Projects/personal/burrow && grep -c "burrow-tools.cjs load" .claude/burrow/workflows/burrow.md && grep -c "lazy body-fetching" .claude/burrow/workflows/burrow.md && grep -c "JSON envelope" .claude/burrow/workflows/burrow.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/burrow/workflows/burrow.md contains `node .claude/burrow/burrow-tools.cjs load` in Step 1
    - .claude/burrow/workflows/burrow.md contains "lazy body-fetching" documentation (WFL-06)
    - .claude/burrow/workflows/burrow.md contains "JSON envelope" description
    - .claude/burrow/workflows/burrow.md contains mode behavior descriptions for full, index, none, auto
    - .claude/burrow/workflows/burrow.md Step 1 does NOT contain "Read tool" as the primary loading mechanism (old pattern removed)
    - .claude/burrow/workflows/burrow.md worked examples reference `burrow load` instead of Read tool for LOAD step
    - .claude/burrow/workflows/burrow.md Command Reference table includes `load` row
    - .claude/burrow/workflows/burrow.md Step 2 (THINK) and Step 3 (EXECUTE) core logic unchanged
    - grep for "Read \`.planning/burrow/cards.json\`" returns 0 matches (old LOAD pattern fully replaced)
  </acceptance_criteria>
  <done>Workflow LOAD step uses burrow load command, documents all four modes, includes lazy body-fetching pattern, worked examples updated, no references to old Read-tool-based loading remain</done>
</task>

</tasks>

<verification>
1. `grep -c "burrow-tools.cjs load" .claude/burrow/workflows/burrow.md` — returns 2+ (Step 1 + at least one example)
2. `grep -c "lazy body-fetching" .claude/burrow/workflows/burrow.md` — returns 1+
3. `grep "Read.*cards.json.*Read tool" .claude/burrow/workflows/burrow.md` — returns 0 matches (old pattern gone)
4. `grep -c "JSON envelope" .claude/burrow/workflows/burrow.md` — returns 1+
5. All existing tests still pass: `node --test test/`
</verification>

<success_criteria>
- Workflow Step 1 dispatches via `burrow load` instead of Read tool
- All four modes (full, index, none, auto) documented with agent instructions
- Lazy body-fetching pattern explicitly described for index mode
- Worked examples updated to use new LOAD pattern
- No references to old Read-tool-based LOAD pattern remain
</success_criteria>

<output>
After completion, create `.planning/phases/16-workflow-load-step-load-command/16-02-SUMMARY.md`
</output>
