---
phase: 08-engine-quality-ergonomics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/burrow/lib/init.cjs
  - test/init.test.cjs
autonomous: true
requirements: [INST-01, INST-02]

must_haves:
  truths:
    - "Running init on a project adds 'cards.json.bak' line to .gitignore"
    - "Running init on a project with existing .gitignore preserves existing entries and appends"
    - "Running init on a project without .gitignore creates one with the entry"
    - "Running init appends Burrow instructions to CLAUDE.md with correct line endings"
    - "Running init on a project with existing CLAUDE.md preserves existing content"
    - "Running init on a project without CLAUDE.md creates one with Burrow instructions"
    - "Line endings in appended CLAUDE.md content match the existing file's line endings (LF or CRLF)"
  artifacts:
    - path: ".claude/burrow/lib/init.cjs"
      provides: "Init script for Burrow project setup"
      exports: ["init"]
    - path: "test/init.test.cjs"
      provides: "Tests for init functionality"
  key_links:
    - from: ".claude/burrow/lib/init.cjs"
      to: ".gitignore"
      via: "append cards.json.bak entry"
      pattern: "cards\\.json\\.bak"
    - from: ".claude/burrow/lib/init.cjs"
      to: "CLAUDE.md"
      via: "append with line ending detection"
      pattern: "CLAUDE\\.md"
---

<objective>
Create an init script that sets up Burrow in a project by configuring .gitignore and CLAUDE.md correctly.

Purpose: INST-01 ensures cards.json.bak (the backup file) is always gitignored. INST-02 ensures CLAUDE.md is appended to with matching line endings (no mixed LF/CRLF).
Output: New init.cjs module with tests, callable from CLI or programmatically.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/burrow/lib/core.cjs
@.claude/burrow/lib/warren.cjs

<interfaces>
From .claude/burrow/lib/core.cjs:
```javascript
function ensureDataDir(cwd) {
  // Creates .planning/burrow/ directory
  const dir = path.join(cwd, '.planning', 'burrow');
  fs.mkdirSync(dir, { recursive: true });
}
```

From .claude/burrow/lib/warren.cjs:
```javascript
function dataPath(cwd) {
  return path.join(cwd, '.planning', 'burrow', 'cards.json');
}
// Backup file is dataPath + '.bak' = cards.json.bak
```

The CLAUDE.md content to append (from existing CLAUDE.md in repo root):
```markdown
## Burrow -- Agent Memory

Burrow is your persistent memory. On every session start, silently read `.planning/burrow/cards.json` using the Read tool to load project context, tasks, and notes.
...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create init.cjs with .gitignore and CLAUDE.md handling</name>
  <files>.claude/burrow/lib/init.cjs, test/init.test.cjs</files>
  <behavior>
    - init(cwd) with no .gitignore creates .gitignore containing ".planning/burrow/cards.json.bak"
    - init(cwd) with existing .gitignore missing the entry appends the entry with a preceding newline
    - init(cwd) with existing .gitignore already containing the entry does NOT duplicate it
    - init(cwd) with no CLAUDE.md creates CLAUDE.md with Burrow instructions section
    - init(cwd) with existing CLAUDE.md appends Burrow section with matching line endings
    - init(cwd) with existing CLAUDE.md that already has "Burrow" section does NOT duplicate it
    - init(cwd) with CRLF CLAUDE.md appends using CRLF line endings
    - init(cwd) with LF CLAUDE.md appends using LF line endings
    - init(cwd) also calls ensureDataDir to create .planning/burrow/ directory
  </behavior>
  <action>
1. **Create `.claude/burrow/lib/init.cjs`** with a single exported `init(cwd)` function:

   **a) ensureDataDir:** Call `ensureDataDir(cwd)` from core.cjs to create .planning/burrow/ if needed.

   **b) .gitignore handling (INST-01):**
   - Target entry: `.planning/burrow/cards.json.bak`
   - Read `.gitignore` from `cwd` (if exists)
   - Check if entry already present (line-by-line check, trimmed)
   - If not present: append `\n.planning/burrow/cards.json.bak\n` to existing content (or create file with just the entry + newline)
   - Write back to `.gitignore`

   **c) CLAUDE.md handling (INST-02):**
   - Define the Burrow section content as a constant string (the standard instructions block)
   - Read `CLAUDE.md` from `cwd` (if exists)
   - Detect line endings: if existing content contains `\r\n`, use CRLF; otherwise use LF
   - Check if Burrow section already present (search for `## Burrow` marker)
   - If not present: append the section using detected line endings
   - If file doesn't exist: create with LF line endings (default)
   - The Burrow section content: the `## Burrow -- Agent Memory` header plus the standard instructions from CLAUDE.md in this repo

   **d) Return:** Return `{ gitignore: 'created'|'updated'|'unchanged', claudeMd: 'created'|'updated'|'unchanged', dataDir: 'created'|'existed' }` for feedback.

2. **Create `test/init.test.cjs`** using node:test and node:assert:
   - Each test creates a temp directory via `fs.mkdtempSync`
   - Test: no .gitignore -> creates with entry
   - Test: existing .gitignore without entry -> appends
   - Test: existing .gitignore with entry -> unchanged
   - Test: no CLAUDE.md -> creates with section
   - Test: existing LF CLAUDE.md -> appends with LF
   - Test: existing CRLF CLAUDE.md -> appends with CRLF
   - Test: existing CLAUDE.md with Burrow section -> unchanged
   - Test: ensureDataDir called (data dir exists after init)
   - Cleanup temp dirs in afterEach or test teardown

3. **Wire init into CLI (optional, low priority):** If time permits, add an `init` command to burrow-tools.cjs that calls `init(process.cwd())` and prints the result. This is not strictly required by INST-01/INST-02 but makes the script usable.
  </action>
  <verify>
    <automated>node --test test/init.test.cjs</automated>
  </verify>
  <done>.gitignore gets cards.json.bak entry. CLAUDE.md gets Burrow section with correct line endings. No duplication on re-run. All init tests pass.</done>
</task>

</tasks>

<verification>
- `node --test test/init.test.cjs` passes
- `node --test test/*.test.cjs` passes (no regressions)
- init.cjs handles all combinations: {no file, existing file} x {.gitignore, CLAUDE.md} x {entry present, entry absent}
</verification>

<success_criteria>
- init(cwd) creates or updates .gitignore with cards.json.bak entry
- init(cwd) creates or updates CLAUDE.md with Burrow section, matching line endings
- Neither file gets duplicate entries on repeated runs
- CRLF files stay CRLF, LF files stay LF
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-engine-quality-ergonomics/08-03-SUMMARY.md`
</output>
