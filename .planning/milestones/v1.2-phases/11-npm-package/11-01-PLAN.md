---
phase: 11-npm-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - install.cjs
autonomous: true
requirements: [NPM-01, NPM-02, NPM-03]

must_haves:
  truths:
    - "npx create-burrow runs the guided installer end-to-end"
    - "The published package contains only source files, commands, and installer"
    - "Running npx create-burrow --help prints usage information with npx examples"
  artifacts:
    - path: "package.json"
      provides: "npm package manifest with name, bin, files whitelist"
      contains: "create-burrow"
    - path: "install.cjs"
      provides: "Updated --help text referencing npx create-burrow"
      contains: "npx create-burrow"
  key_links:
    - from: "package.json"
      to: "install.cjs"
      via: "bin field"
      pattern: '"bin".*install\\.cjs'
    - from: "package.json"
      to: ".claude/burrow/"
      via: "files whitelist"
      pattern: '"files"'
---

<objective>
Create the npm package manifest for `create-burrow` so that `npx create-burrow` runs the guided installer, the package ships only necessary files, and `--help` output reflects the npx usage pattern.

Purpose: This is the final phase of v1.2 — making burrow publicly installable via npm without git clone.
Output: package.json with bin/files config, updated --help text in install.cjs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@install.cjs
@.claude/burrow/VERSION
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and update --help text</name>
  <files>package.json, install.cjs</files>
  <read_first>
    - install.cjs (current --help text at printUsage function, line ~117; current require paths at lines 8-18; current shebang line 1)
    - .claude/burrow/VERSION (current version string: 1.2.0)
    - LICENSE (confirm MIT license exists)
  </read_first>
  <action>
Create `package.json` in the repo root with the following exact content:

```json
{
  "name": "create-burrow",
  "version": "1.2.0",
  "description": "Infinitely nestable Trello for AI agents. Persistent card-based memory for Claude Code.",
  "license": "MIT",
  "author": "mrmatos",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mrmatos/burrow.git"
  },
  "bin": {
    "create-burrow": "install.cjs"
  },
  "files": [
    "install.cjs",
    ".claude/burrow/",
    ".claude/commands/burrow/"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "ai-agent",
    "memory",
    "trello",
    "cards"
  ],
  "engines": {
    "node": ">=18"
  }
}
```

Key points about the `files` whitelist (NPM-02):
- `install.cjs` — the CLI entry point and bin target
- `.claude/burrow/` — all source code (lib/, burrow-tools.cjs, VERSION, workflows/)
- `.claude/commands/burrow/` — slash command markdown files
- Excludes: test/, .planning/, field-reports/, README.md, CLAUDE.md, .claude/get-shit-done/, .claude/hooks/, .claude/agents/, .claude/commands/gsd/

Note: `package.json` and `LICENSE` are always included by npm regardless of the `files` list.

Then update the `printUsage()` function in `install.cjs` (around line 117-143). Replace the current usage text with:

```
Usage: npx create-burrow [target-dir] [options]

  Installs (or manages) Burrow in the target project directory.

Arguments:
  target-dir        Path to the target project root (default: current directory)

Options:
  --yes, -y         Non-interactive mode. Accept all defaults, skip prompts.
  --uninstall       Remove all Burrow files from the target project.
  --help, -h        Print this help message and exit.

Modes (auto-detected):
  fresh install     No Burrow files found — installs everything fresh.
  upgrade           All Burrow files present — replaces source files, preserves data.
  repair            Some Burrow files missing — restores only missing files.
  uninstall         Removes .claude/burrow/, .claude/commands/burrow*, .planning/burrow/, and the CLAUDE.md sentinel block.

Examples:
  npx create-burrow                   Interactive install in current directory
  npx create-burrow /path/to/project  Interactive install in specified directory
  npx create-burrow --yes             Non-interactive install with all defaults
  npx create-burrow --uninstall       Interactive uninstall (requires confirmation)
  npx create-burrow --uninstall --yes Non-interactive uninstall
```

Only change "node install.cjs" to "npx create-burrow" in the usage text. Do NOT change the shebang line (already `#!/usr/bin/env node`). Do NOT change any require paths or logic.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const assert = require('node:assert'); assert.strictEqual(pkg.name, 'create-burrow'); assert.strictEqual(pkg.version, '1.2.0'); assert.strictEqual(pkg.bin['create-burrow'], 'install.cjs'); assert(pkg.files.includes('install.cjs')); assert(pkg.files.includes('.claude/burrow/')); assert(pkg.files.includes('.claude/commands/burrow/')); assert.strictEqual(pkg.files.length, 3); console.log('package.json OK')" && node install.cjs --help 2>&1 | grep -q "npx create-burrow" && echo "--help OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists at repo root with `"name": "create-burrow"`
    - package.json contains `"version": "1.2.0"`
    - package.json contains `"bin": { "create-burrow": "install.cjs" }`
    - package.json `files` array has exactly 3 entries: `install.cjs`, `.claude/burrow/`, `.claude/commands/burrow/`
    - package.json contains `"license": "MIT"`
    - package.json contains `"engines": { "node": ">=18" }`
    - install.cjs printUsage() output contains "npx create-burrow" (not "node install.cjs")
    - install.cjs printUsage() output contains all 5 example lines with "npx create-burrow"
    - install.cjs shebang line is still `#!/usr/bin/env node`
    - install.cjs require paths unchanged (still `./.claude/burrow/lib/installer.cjs` and `./.claude/burrow/lib/version.cjs`)
    - All existing tests still pass: `node --test test/*.test.cjs`
  </acceptance_criteria>
  <done>package.json created with correct npm package config. install.cjs --help shows npx create-burrow usage. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify npm pack contents match whitelist</name>
  <files></files>
  <read_first>
    - package.json (verify files whitelist before packing)
  </read_first>
  <action>
Run `npm pack --dry-run` to verify the package contents match the whitelist. The output lists every file that would be included in the tarball.

**Expected files (must be present):**
- package.json (always included by npm)
- LICENSE (always included by npm)
- install.cjs
- .claude/burrow/VERSION
- .claude/burrow/burrow-tools.cjs
- .claude/burrow/lib/core.cjs
- .claude/burrow/lib/init.cjs
- .claude/burrow/lib/installer.cjs
- .claude/burrow/lib/mongoose.cjs
- .claude/burrow/lib/render.cjs
- .claude/burrow/lib/version.cjs
- .claude/burrow/lib/warren.cjs
- .claude/burrow/workflows/burrow.md
- .claude/commands/burrow/add.md
- .claude/commands/burrow/archive.md
- .claude/commands/burrow/dump.md
- .claude/commands/burrow/edit.md
- .claude/commands/burrow/help.md
- .claude/commands/burrow/move.md
- .claude/commands/burrow/read.md
- .claude/commands/burrow/remove.md
- .claude/commands/burrow/unarchive.md
- .claude/commands/burrow/update.md

**Must NOT be present (verify exclusion):**
- No files from `test/` directory
- No files from `.planning/` directory
- No files from `field-reports/` directory
- No `README.md` (unless npm forces it — check output)
- No `CLAUDE.md`
- No files from `.claude/get-shit-done/`
- No files from `.claude/hooks/`
- No files from `.claude/agents/`
- No files from `.claude/commands/gsd/`

Run `npm pack --dry-run 2>&1` and verify the file list. If any unexpected files appear, diagnose whether the `files` whitelist needs adjustment. If `README.md` appears (npm includes it automatically), that is acceptable — it is a small file and standard npm practice.

Also run `node install.cjs --help` one final time to confirm the help text shows `npx create-burrow`.

Also run all existing tests to confirm nothing is broken: `node --test test/*.test.cjs`
  </action>
  <verify>
    <automated>npm pack --dry-run 2>&1 | grep -v "^npm" | grep -c "/" | xargs -I{} test {} -ge 20 && npm pack --dry-run 2>&1 | grep -qv "test/" && echo "No test files" && npm pack --dry-run 2>&1 | grep -qv ".planning/" && echo "No planning files" && node --test test/*.test.cjs 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `npm pack --dry-run` output includes install.cjs
    - `npm pack --dry-run` output includes .claude/burrow/burrow-tools.cjs
    - `npm pack --dry-run` output includes .claude/burrow/lib/installer.cjs
    - `npm pack --dry-run` output includes .claude/commands/burrow/add.md
    - `npm pack --dry-run` output does NOT include any file matching `test/`
    - `npm pack --dry-run` output does NOT include any file matching `.planning/`
    - `npm pack --dry-run` output does NOT include any file matching `field-reports/`
    - `npm pack --dry-run` output does NOT include any file matching `.claude/get-shit-done/`
    - `npm pack --dry-run` output does NOT include any file matching `.claude/agents/`
    - `npm pack --dry-run` output does NOT include CLAUDE.md
    - `node --test test/*.test.cjs` exits 0 (all tests pass)
    - `node install.cjs --help` output contains "npx create-burrow"
  </acceptance_criteria>
  <done>npm pack --dry-run confirms only whitelisted files are included. No test, planning, or internal tooling files leak into the package. All tests pass.</done>
</task>

</tasks>

<verification>
1. `node -e "const p=require('./package.json'); console.log(p.name, p.version, p.bin)"` shows create-burrow 1.2.0 with install.cjs bin
2. `npm pack --dry-run` shows only expected files (source, commands, installer, LICENSE, package.json)
3. `node install.cjs --help` shows npx create-burrow usage
4. `node --test test/*.test.cjs` all pass
</verification>

<success_criteria>
- package.json exists with name=create-burrow, bin pointing to install.cjs, files whitelist of 3 entries
- npm pack --dry-run shows ~22 files (source + commands + installer + package.json + LICENSE)
- --help output uses npx create-burrow in all examples
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-npm-package/11-01-SUMMARY.md`
</output>
