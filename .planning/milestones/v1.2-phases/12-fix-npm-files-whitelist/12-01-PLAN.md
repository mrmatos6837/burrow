---
phase: 12-fix-npm-files-whitelist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - NPM-02
must_haves:
  truths:
    - ".claude/commands/burrow.md is included in the npm tarball"
    - "npx create-burrow does not crash with ENOENT on burrow.md copy step"
  artifacts:
    - path: "package.json"
      provides: "files whitelist including burrow.md"
      contains: ".claude/commands/burrow.md"
  key_links:
    - from: "package.json files array"
      to: "install.cjs line 162 (commandFile)"
      via: "npm pack includes file that installer copies"
      pattern: '\.claude/commands/burrow\.md'
---

<objective>
Add the missing `.claude/commands/burrow.md` entry to the `files` whitelist in `package.json` so that `npm pack` includes it in the tarball and `npx create-burrow` no longer crashes with ENOENT when the installer tries to copy it.

Purpose: Gap closure from v1.2 milestone audit — the installer references this file at line 162 but the npm package excludes it.
Output: Updated package.json with corrected files whitelist.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add burrow.md to package.json files whitelist and verify tarball</name>
  <files>package.json</files>
  <read_first>
    - package.json (current files array has 3 entries: install.cjs, .claude/burrow/, .claude/commands/burrow/)
    - install.cjs (line 162 defines commandFile: '.claude/commands/burrow.md' — this is what breaks)
  </read_first>
  <action>
Edit `package.json` to add `".claude/commands/burrow.md"` to the `files` array. The resulting array should be:

```json
"files": [
  "install.cjs",
  ".claude/burrow/",
  ".claude/commands/burrow/",
  ".claude/commands/burrow.md"
]
```

After editing, run `npm pack --dry-run` and confirm `.claude/commands/burrow.md` appears in the output.
  </action>
  <verify>
    <automated>cd /Users/mrmatos6837/Projects/personal/burrow && npm pack --dry-run 2>&1 | grep 'commands/burrow.md'</automated>
  </verify>
  <acceptance_criteria>
    - package.json `files` array contains exactly 4 entries
    - package.json contains the string `".claude/commands/burrow.md"`
    - `npm pack --dry-run` output includes `.claude/commands/burrow.md`
  </acceptance_criteria>
  <done>.claude/commands/burrow.md appears in npm pack --dry-run output, confirming the npm tarball will include the file the installer needs</done>
</task>

</tasks>

<verification>
1. `grep 'burrow.md' package.json` shows the new entry in files array
2. `npm pack --dry-run 2>&1 | grep 'commands/burrow.md'` returns a match
3. No other files in package.json were modified (only the files array changed)
</verification>

<success_criteria>
- `.claude/commands/burrow.md` appears in `npm pack --dry-run` output
- `npx create-burrow` will no longer crash with ENOENT on the burrow.md copy step (the file is now in the tarball)
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-npm-files-whitelist/12-01-SUMMARY.md`
</output>
