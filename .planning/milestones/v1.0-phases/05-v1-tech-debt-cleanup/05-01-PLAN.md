---
phase: 05-v1-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/burrow/bin/init.cjs
  - .claude/burrow/test/render.test.cjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DATA-02, DATA-09, PP-03, PP-06, CLI-01, RNDR-01, CLI-03, CMDS-03]
gap_closure: true

must_haves:
  truths:
    - "init.cjs writes {version:2,cards:[]} when creating a new cards.json"
    - "All 134 tests pass with 0 failures"
    - "REQUIREMENTS.md text matches current codebase (no references to --json flag, get command, /burrow:show, /burrow:delete)"
    - "ROADMAP.md shows Phase 2 and Phase 4 as complete"
    - "No empty stale phase directories exist"
    - "Out of Scope table clarifies find vs search distinction"
  artifacts:
    - path: ".claude/burrow/bin/init.cjs"
      provides: "Correct v2 format for new project initialization"
      contains: "version.*2.*cards"
    - path: ".claude/burrow/test/render.test.cjs"
      provides: "Updated tests using renderMutation('remove')"
      contains: "renderMutation.*remove"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled requirement text matching codebase"
      contains: "read is the universal view command"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate completion status for all phases"
      contains: "\\[x\\].*Phase 2"
  key_links:
    - from: ".claude/burrow/bin/init.cjs"
      to: ".claude/burrow/lib/warren.cjs"
      via: "init writes format that load() expects"
      pattern: "version.*2.*cards"
    - from: ".claude/burrow/test/render.test.cjs"
      to: ".claude/burrow/lib/render.cjs"
      via: "test calls match render.cjs case labels"
      pattern: "renderMutation.*remove"
---

<objective>
Close all tech debt gaps identified by v1.0 milestone audit: fix the init.cjs data format bug, update 2 stale render tests, reconcile 5 stale requirement descriptions, update ROADMAP.md completion status, clarify find vs search in Out of Scope, and remove an empty stale directory.

Purpose: Bring documentation and tests into alignment with the codebase after intentional refactors (get->read, delete->remove, --json removal).
Output: All 134 tests green, documentation matches code, clean filesystem.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-v1-tech-debt-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix init.cjs data format and stale render tests</name>
  <files>.claude/burrow/bin/init.cjs, .claude/burrow/test/render.test.cjs</files>
  <action>
Two code fixes:

1. **init.cjs line 85** — Change `fs.writeFileSync(dataFile, '[]\n')` to `fs.writeFileSync(dataFile, JSON.stringify({ version: 2, cards: [] }) + '\n')`. This ensures new projects initialized via init.cjs get the v2 format that `warren.cjs load()` expects.

2. **render.test.cjs lines 304-316** — Fix two stale tests that call `renderMutation('delete')` (render.cjs only handles `'remove'` after the rename refactor):
   - Line 304: Change test name from `'renders delete as one-liner'` to `'renders remove as one-liner'`
   - Line 305: Change `renderMutation('delete', ...)` to `renderMutation('remove', ...)`
   - Line 306: Change `assert.ok(result.includes('\u2713 Deleted'))` to `assert.ok(result.includes('\u2713 Removed'))`
   - Line 312: Change test name from `'omits child count when zero for delete'` to `'omits child count when zero for remove'`
   - Line 313: Change `renderMutation('delete', ...)` to `renderMutation('remove', ...)`
   - Line 314: Change `assert.ok(result.includes('\u2713 Deleted'))` to `assert.ok(result.includes('\u2713 Removed'))`
  </action>
  <verify>
    <automated>node --test .claude/burrow/test/</automated>
  </verify>
  <done>init.cjs writes valid v2 JSON format. All 134 tests pass with 0 failures (the 2 previously failing render tests now pass).</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile documentation drift and clean stale artifacts</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Six documentation fixes:

1. **REQUIREMENTS.md — Update 5 stale requirement descriptions:**
   - RNDR-01 (line 30): Change `get is the universal view command -- get [id] [--depth N]` to `read is the universal view command -- read [id] [--depth N] covers all read operations`
   - PP-06 (line 51): Change `--json flag on any command bypasses render and returns raw structured JSON (previous default becomes opt-in)` to `Pretty-print is the only output mode -- no --json flag (removed in v1.0)`
   - CLI-01 (line 59): Change `CLI helper (burrow-tools.cjs) returns structured JSON for all operations` to `CLI helper (burrow-tools.cjs) returns pretty-printed text for all operations`
   - CLI-03 (line 61): Change `get replaces list/dump/children as universal view -- aliases for backward compat (list = get no args, dump = get --depth 0, children = get <id>)` to `read replaces list/dump/children as universal view -- aliases for backward compat (list = read no args, dump = read --depth 0, children = read <id>)`
   - CMDS-03 (line 69): Change `/burrow:show shortcut for viewing tree at specified depth/focus` to `/burrow:read shortcut for viewing tree at specified depth/focus`

2. **REQUIREMENTS.md — Update stale note on line 72:**
   Change the note referencing `/burrow:delete` to reference `/burrow:remove`.

3. **REQUIREMENTS.md — Clarify Out of Scope table (line 109):**
   Change the "Search command" row to read "Search / query engine" with the reason updated to clarify that `find` (simple title-match lookup) exists as a lightweight command, while a full search/query engine is out of scope.

4. **ROADMAP.md — Check Phase 2 and Phase 4 completion boxes:**
   - Line 16: `- [ ] **Phase 2:` to `- [x] **Phase 2:`
   - Line 18: `- [ ] **Phase 4:` to `- [x] **Phase 4:`
   - Phase 2 plan list: `- [ ] 02-01-PLAN.md` to `- [x] 02-01-PLAN.md` and `- [ ] 02-02-PLAN.md` to `- [x] 02-02-PLAN.md`
   - Phase 4 plan list: `- [ ] 04-01-PLAN.md` to `- [x] 04-01-PLAN.md` and `- [ ] 04-02-PLAN.md` to `- [x] 04-02-PLAN.md`

5. **ROADMAP.md — Update Phase 5 plans count:**
   Change `**Plans**: 0 plans` to `**Plans**: 1 plan`

6. **Remove stale directory:**
   Run `rm -rf .planning/phases/04-cli-pretty-print-rendering-with-json-flag/`
  </action>
  <verify>
    <automated>grep -c "get is the universal" .planning/REQUIREMENTS.md && echo "FAIL: stale get reference" || echo "PASS: no stale get"; grep -c "\-\-json flag on any command" .planning/REQUIREMENTS.md && echo "FAIL: stale --json" || echo "PASS: no stale --json"; grep -c "burrow:show" .planning/REQUIREMENTS.md && echo "FAIL: stale show" || echo "PASS: no stale show"; grep -c "burrow:delete" .planning/REQUIREMENTS.md && echo "FAIL: stale delete" || echo "PASS: no stale delete"; grep -c "\- \[ \] \*\*Phase 2" .planning/ROADMAP.md && echo "FAIL: Phase 2 unchecked" || echo "PASS: Phase 2 checked"; grep -c "\- \[ \] \*\*Phase 4" .planning/ROADMAP.md && echo "FAIL: Phase 4 unchecked" || echo "PASS: Phase 4 checked"; test -d .planning/phases/04-cli-pretty-print-rendering-with-json-flag && echo "FAIL: stale dir exists" || echo "PASS: stale dir removed"</automated>
  </verify>
  <done>All 5 requirement descriptions match current codebase. Out of Scope clarifies find vs search. ROADMAP.md shows Phase 2 and 4 as complete with all plan checkboxes checked. Empty stale directory removed. Zero references to --json flag, get command (as CLI command name), /burrow:show, or /burrow:delete in requirement text.</done>
</task>

</tasks>

<verification>
1. `node --test .claude/burrow/test/` — all 134 tests pass, 0 failures
2. No stale references in REQUIREMENTS.md: no `--json flag`, no `get` as command name, no `/burrow:show`, no `/burrow:delete`
3. ROADMAP.md Phase 2 and 4 marked complete
4. Stale directory `.planning/phases/04-cli-pretty-print-rendering-with-json-flag/` does not exist
5. Out of Scope table distinguishes `find` (exists) from search engine (out of scope)
</verification>

<success_criteria>
- 134/134 tests pass
- init.cjs writes `{"version":2,"cards":[]}` for new projects
- All requirement text matches current codebase state
- ROADMAP.md accurately reflects completion status
- No stale artifacts remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-v1-tech-debt-cleanup/05-01-SUMMARY.md`
</output>
