---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
must_haves:
  truths:
    - "README shows npx create-burrow as primary install method"
    - "README documents the interactive installer (detect, upgrade, repair, uninstall)"
    - "README includes /burrow:update in shortcut command list"
    - "README shows correct Node.js requirement (>=18)"
    - "README shows MIT license instead of TBD"
    - "README mentions --yes flag for non-interactive install"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation reflecting v1.2 changes"
  key_links: []
---

<objective>
Update README.md to reflect v1.2 (Packaging & Distribution) changes.

Purpose: The README still shows the pre-npm install method (git clone + node install.cjs) and is missing v1.2 features like npx create-burrow, interactive installer, /burrow:update command, and --yes flag. License also needs updating from TBD to MIT.

Output: Updated README.md accurate to current state.
</objective>

<execution_context>
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrmatos6837/Projects/personal/burrow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@package.json
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for v1.2</name>
  <files>README.md</files>
  <action>
Read the current README.md fully, then make the following targeted updates:

1. **Setup section** — Replace the git clone install method with npx as primary:
   ```bash
   npx create-burrow
   ```
   Run it from inside your project directory. The interactive installer detects existing installations and offers: fresh install, upgrade, repair, or uninstall.

   Keep the git clone method as a secondary option under a "Manual install" subsection for users who prefer it:
   ```bash
   git clone https://github.com/mrmatos6837/burrow.git
   node burrow/install.cjs ~/your-project
   ```

   Add note about `--yes` flag for non-interactive install (CI, scripting):
   ```bash
   npx create-burrow --yes
   ```

2. **Requirements** — Change "Node.js v19+" to "Node.js 18+" (matches package.json engines field).

3. **Shortcut commands list** — Add `/burrow:update` to the list of shortcut commands. It should appear in a natural position (e.g., after `/burrow:help` or grouped logically).

4. **License section** — Change "TBD" to "MIT" at the bottom.

5. **Uninstall section** — Add a note that `npx create-burrow` also offers uninstall as an option in its interactive menu, in addition to the manual rm -rf approach.

Do NOT change:
- The opening pitch/personality text (hole in the ground, etc.)
- The "How it works" tree example
- The CLI Reference section (commands, flags, schemas are all current)
- The "Why" section
- The FAQ section (except if a FAQ answer references the old install method)
- The Agent Memory section

Keep the same tone and style as the existing README — direct, no fluff, slightly irreverent.
  </action>
  <verify>
    <automated>grep -q "npx create-burrow" README.md && grep -q "MIT" README.md && grep -q "burrow:update" README.md && grep -q "18+" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Setup section leads with `npx create-burrow`, mentions interactive installer and --yes flag
    - Git clone kept as manual alternative
    - Node.js requirement says 18+
    - /burrow:update appears in shortcut commands
    - License says MIT
    - Uninstall mentions npx option
  </done>
</task>

</tasks>

<verification>
- README.md renders correctly (no broken markdown)
- All v1.2 features are represented
- No factual errors introduced
</verification>

<success_criteria>
README accurately reflects the current state of Burrow v1.2 including npm distribution, interactive installer, and all commands.
</success_criteria>

<output>
After completion, create `.planning/quick/260330-f9e-update-the-readme-to-reflect-the-most-re/260330-f9e-SUMMARY.md`
</output>
