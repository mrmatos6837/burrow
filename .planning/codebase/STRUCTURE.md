# Codebase Structure

**Analysis Date:** 2026-03-06

## Directory Layout

```
todobox/
├── .claude/                    # Claude Code tooling configuration
│   ├── agents/                 # GSD agent definitions (12 agents)
│   ├── commands/
│   │   └── gsd/                # GSD slash commands (30+ commands)
│   ├── get-shit-done/          # GSD framework core
│   │   ├── bin/                # CLI tool and library modules
│   │   │   ├── gsd-tools.cjs   # Main CLI entry point
│   │   │   └── lib/            # Library modules (commands, config, core, etc.)
│   │   ├── references/         # Reference documentation for GSD
│   │   ├── templates/          # Templates for milestones, phases, etc.
│   │   │   └── codebase/       # Codebase mapping templates
│   │   └── workflows/          # Workflow definitions for each command
│   └── hooks/                  # Claude session hooks
├── .planning/                  # Project planning artifacts
│   └── codebase/               # Codebase analysis documents (this file)
└── .git/                       # Git repository
```

## Directory Purposes

**`.claude/`:**
- Purpose: Claude Code integration and GSD workflow tooling
- Contains: Agent definitions, slash commands, workflow scripts, hooks
- Key files: `.claude/settings.json` (hooks configuration)

**`.claude/agents/`:**
- Purpose: GSD agent role definitions for specialized tasks
- Contains: 12 agent markdown files (mapper, debugger, executor, planner, verifier, etc.)
- Key files: `gsd-codebase-mapper.md`, `gsd-executor.md`, `gsd-planner.md`

**`.claude/commands/gsd/`:**
- Purpose: Slash command definitions for GSD workflow
- Contains: 30+ command markdown files
- Key files: `execute-phase.md`, `plan-phase.md`, `new-project.md`, `new-milestone.md`

**`.claude/get-shit-done/bin/`:**
- Purpose: GSD CLI tool implementation
- Contains: Node.js CommonJS modules
- Key files: `gsd-tools.cjs` (entry point), `lib/core.cjs`, `lib/commands.cjs`, `lib/config.cjs`

**`.claude/get-shit-done/bin/lib/`:**
- Purpose: Library modules for GSD CLI
- Contains: `commands.cjs`, `config.cjs`, `core.cjs`, `frontmatter.cjs`, `init.cjs`, `milestone.cjs`, `phase.cjs`, `roadmap.cjs`, `state.cjs`, `template.cjs`, `verify.cjs`

**`.planning/`:**
- Purpose: Project planning and codebase analysis artifacts
- Contains: Codebase mapping documents
- Generated: Yes (by GSD mapping commands)
- Committed: Yes

## Key File Locations

**Entry Points:**
- No application entry points exist yet

**Configuration:**
- `.claude/settings.json`: Claude hooks and status line configuration

**Core Logic:**
- No application logic exists yet

**Testing:**
- No test infrastructure exists yet

## Naming Conventions

**No application conventions established yet.** When adding application code:
- Follow conventions documented in CONVENTIONS.md (to be written by quality mapper)
- Project name "todobox" suggests a todo/task management domain

## Where to Add New Code

**New Application Code:**
- Create appropriate root-level directories (e.g., `src/`, `app/`, `lib/`) based on chosen framework
- Add `package.json`, `tsconfig.json`, or equivalent at project root

**New Feature:**
- Depends on framework choice; update this document after initial scaffolding

**New Component/Module:**
- Depends on framework choice; update this document after initial scaffolding

**Utilities:**
- Depends on framework choice; update this document after initial scaffolding

**Tests:**
- Depends on framework choice; update this document after initial scaffolding

## Special Directories

**`.claude/`:**
- Purpose: GSD tooling framework - do not modify manually
- Generated: Installed by GSD
- Committed: Yes

**`.planning/`:**
- Purpose: Planning artifacts generated by GSD commands
- Generated: Yes
- Committed: Yes

**`.planning/codebase/`:**
- Purpose: Codebase analysis documents (ARCHITECTURE.md, STRUCTURE.md, etc.)
- Generated: Yes (by `map-codebase` command)
- Committed: Yes

---

*Structure analysis: 2026-03-06*
